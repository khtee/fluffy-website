version: '3.8'

services:
  fluffy-website:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fluffy-cat-hotel
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
